apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "second_chart.fullname" . }}-test-connection"
  labels:
    {{- include "second_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "second_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
